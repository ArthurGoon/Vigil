DATABASE_URL=postgres://vigil:vigil@localhost:5432/vigil
PORT=3000
APP_URL=http://localhost:3000
SESSION_SECRET=change-me-to-a-long-random-string
RESEND_API_KEY=
MAGIC_LINK_FROM=Vigil <onboarding@resend.dev>
# When RESEND_API_KEY is empty, magic links / alert emails are logged to console (dev mode)

# Google OAuth (recommended for product login + alert emails)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional override; default is ${APP_URL}/auth/google/callback
GOOGLE_REDIRECT_URI=
# Optional: Chrome extension OAuth client ID if different from web
GOOGLE_EXTENSION_CLIENT_ID=

WORKER_POLL_MS=5000
WORKER_CONCURRENCY=3
